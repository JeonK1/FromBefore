<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frombefore">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".EndingMessageAllActivity"></activity>
        <activity android:name=".EndingMessageActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".CalendarAnimateActivity" />
        <activity android:name=".DaySelectActivity" />
        <activity android:name=".GifTestActivity" />
        <activity android:name=".MsgSendToMeActivity" />
        <activity android:name=".InitDdayActivity" />
        <activity android:name=".InitMessageActivity" />
        <activity android:name=".CalendarActivity" />
        <activity android:name=".EndingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>