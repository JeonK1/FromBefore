<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.frombefore">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".message.ReceiveMessageActivity"></activity>
        <activity android:name=".manager.EndingActivity"></activity>
        <activity android:name=".manager.EndingMessageAllActivity" />
        <activity android:name=".manager.EndingMessageActivity" />
        <activity android:name=".message.WriteMessage" />
        <activity android:name=".calendar.CalendarAnimateActivity" />
        <activity android:name=".manager.DaySelectActivity" />
        <activity android:name=".message.MsgSendToMeActivity" />
        <activity android:name=".manager.InitDdayActivity" />
        <activity android:name=".manager.InitMessageActivity" />
        <activity android:name=".calendar.CalendarActivity" />
        <activity android:name=".manager.MainLayoutActivity" />
        <activity android:name=".manager.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>